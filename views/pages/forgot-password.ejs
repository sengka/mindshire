<div class="welcome-hero">
  <div class="welcome-content" style="max-width: 500px">
    <% if (typeof success !=='undefined' && success) { %>

      <div class="success-message">
        <div style="font-size: 4rem; margin-bottom: 10px">🦉</div>

        <h1 class="magical-title" style="font-size: 2.5rem">
          Baykuş Yola Çıktı!
        </h1>

        <p style="color: var(--text-main); margin-bottom: 30px; line-height: 1.6">
          Sihirli parşömen (şifre sıfırlama linki) e-posta adresine gönderildi.
          <br />
          <span style="font-size: 0.9rem; color: var(--text-muted)">
            (Gelen kutunu ve Spam klasörünü kontrol etmeyi unutma!)
          </span>
        </p>

        <a href="/giris" class="btn-magic full-width" style="text-decoration: none">
          <span class="icon">🗝️</span> Giriş Ekranına Dön
        </a>
      </div>

      <% } else { %>

        <header class="register-header">
          <h1 class="magical-title" style="font-size: 2.8rem">Şifremi Unuttum</h1>
          <p class="magical-subtitle">BÜYÜLÜ KELİMELER BAZEN UNUTULUR...</p>

          <% if (typeof error !=='undefined' ) { %>
            <div style="
          color: #ff6b6b;
          margin-bottom: 15px;
          font-size: 0.9rem;
          background: rgba(255, 0, 0, 0.1);
          padding: 10px;
          border-radius: 8px;
          border: 1px solid rgba(255, 0, 0, 0.2);
        ">
              ⚠️ <%= error %>
            </div>
            <% } %>
        </header>

        <form action="/sifremi-unuttum" method="POST" class="magic-form">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div class="form-group magic-field">
            <span class="input-icon">📧</span>
            <input type="email" name="email" id="inp-reset-email" class="magic-input" placeholder=" " required />
            <label for="inp-reset-email" class="magic-label">Kayıtlı E-Posta Adresin</label>
          </div>

          <p style="
          color: var(--text-muted);
          font-size: 0.85rem;
          margin: 20px 0;
          line-height: 1.4;
        ">
            E-posta adresini gir. Sana şifreni yenilemen için sihirli bir parşömen
            göndereceğiz.
          </p>

          <button type="submit" class="btn-magic full-width">
            <span class="icon">🦉</span> Baykuşu Gönder
          </button>
        </form>

        <div class="login-redirect-top" style="margin-top: 25px">
          <a href="/giris">↩ Giriş Ekranına Dön</a>
        </div>

        <% } %>
  </div>
</div>